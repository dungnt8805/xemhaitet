import React, { PropTypes } from "react"
import enhanceCollection from "phenomic/lib/enhance-collection"

const CategoryPage = ({ head }) => {
  return (
    <div></div>
  )
}
CategoryPage.propTypes = {
  head: PropTypes.object.isRequired,
}

export default CategoryPage